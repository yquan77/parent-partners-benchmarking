# handoff.md — parent-partners-benchmarking

## ⏯️ 目前做到哪
HTML slide deck已经完整可用、8张真实照片全部到位、内容文字（含"为什么"这条学生自主性主线）已定稿并同步进HTML、数据全部核实过、荧光盾牌揭晓/人形克隆动画/文字位置变体等视觉细节反复调整到 Yong Quan 满意为止。目前deck**可以直接上台用**。今天最后一个动作是把这次做deck的设计经验整理成全局skill `~/.claude/skills/html-slide-deck/`。

## 🚦 目前状态
- ✅ deck本身：可运行，46张slide，已push到GitHub Pages（https://yquan77.github.io/parent-partners-benchmarking/）
- ✅ 现场互动投票：Supabase已接通，实测过
- ⚠️ **上台前必须做**：Supabase SQL editor 跑 `delete from confidence_ratings;` 清空之前测试留下的假投票数据
- ⚠️ Yong Quan 大概率还会给零星视觉反馈（某页padding、文字位置、动画节奏），这是正常迭代节奏，不代表deck有问题，接手时不用重新审查整份deck，等他指出具体哪页再动
- ❌ 马来文同步显示功能：完全还没开始，是这次交接最主要的新任务，见下方

## ➡️ 下一步

### 1.（最优先）马来文同步显示功能

**需求**：Yong Quan全程用中文讲，现场有少数马来老师听不懂。他想要的效果：他讲到哪一页，马来老师自己手机上就能看到那一页对应的马来文重点，不用打断中文分享节奏。

**技术方案（已想清楚，直接执行即可，不用重新设计）**：
deck现有的Supabase project（`confidence_ratings`那个，见agents.md）已经是可用的即时同步管道，不用另外接新服务，只需要加一张表：

1. 建一张新表 `current_slide`（单一行，比如固定 `id=1`，一个欄位 `slide_index int`），RLS开放anon可以select+update（这个不涉及任何敏感数据，公开读写没有安全疑虑，跟评分表的风险等级不同，不用像评分表那样只开insert）
2. 改 `assets/js/deck.js` 的 `go(idx)` 函数：每次翻页时，用跟 `ratings.js` 一样的fetch REST写法，把 `idx` upsert进 `current_slide`（不要让这个请求阻塞翻页本身，失败也不影响讲者操作——fire and forget）
3. 新增一个手机端页面，比如 `my/index.html`（跟现有 `vote/index.html`同一个模式）：
   - 每1.5秒左右poll一次 `current_slide`（复用 `ratings.js` 的写法，不用引入新的library或websocket，够用且简单）
   - 拿到 `slide_index` 后，从本地一个JS对象/陣列查出对应的马来文重点句，显示出来（大字、简洁，不用做成跟主deck一样华丽的动画，这是给人"跟着看重点"用的，不是给人看设计的）
   - 页面上放一个QR码（复用已经vendored好的 `qrcode.min.js`），指向这个页面自己的网址

**这个方案里唯一不是纯工程活、需要人工的部分**：46页每页的马来文重点句翻译还不存在，这是内容任务不是技术任务。建议做法：
   - 不用逐字翻译每页所有文字，只翻每页的"一级重点"那一句（`.lead` / `.hand-term` / 引言原文），够让人跟上重点就好
   - 先用讲稿定稿（`benchmarking-slot/讲稿要点.md`）比对index.html逐页拉出这句话，草拟马来文初稿
   - **草稿务必给 Yong Quan 本人过目再上线**——他是本地老师，马来文是官方教学语言，他自己校对最快也最准，不要自己假设翻译没问题就直接部署

**次要建议**：QR码不用每页都放（会干扰主deck画面），在分享开场口头致谢那段顺带说一句"如果有老师需要马来文对照，可以扫这个码"，QR码可以印在纸卡上现场发，或开场时短暂显示一次就好。

### 2. 持续视觉微调
等 Yong Quan 提出具体哪一页要调整，照之前的模式处理：本地起server（记得先用 `lsof -i :端口` 确认端口没被别的专案占用，这台电脑上常同时有其他project在跑）、用Playwright截图确认效果、改完commit + push。

## ⚠️ 注意事项
- **这台电脑上可能同时有其他不相关专案在跑本地server/浏览器自动化**，之前踩过端口冲突和共用浏览器tab被别的agent操作到的坑——开工前务必自己检查端口内容、开新tab不要抢别人的
- rm权限在这个环境会被拦下，删档案要用 `mv` 搬进 `.trash/`，不要硬删
- 改HTML/CSS前**必须先读index.html顶部的排版规则注释**，那是这份deck的骨架，别凭直觉改

## 🎓 与博士研究（IBSDK）的关系
这次准备benchmarking分享的过程，意外整理出了一批对 Yong Quan 博士论文（IBSDK家长志工入班计划）有直接价值的材料，不只是一场10分钟分享的副产品：

1. **`/Users/yquanloo/Documents/UTM/IBSDK-Research/家长参与活动记录.md`**——三年（2023-2026）逐场活动的家长参与人数记录，含官方原始记录表核对过的数字，是目前最完整的量化参与数据
2. **`/Users/yquanloo/Documents/UTM/IBSDK-Research/家长心得回馈.md`**——这次为了找演讲素材，把散落的家长心得回馈整理成统一格式（按活动分区、`### 姓名` 标注），里面的家长原话（尤其昱杰妈妈那组"不是他们做不到，是我们大人先入为主"的完整心路转折，横跨两次活动、隔五个月）是很扎实的质性材料，直接呼应博士研究"教师训练家长→家长对孩子放手→学生拿回自主性"这条GRR放手模型的因果链
3. **演讲内容本身「为什么要让家长进课堂」这条线**（师生比1:35→1:5、GRR三层放手）是这次为了回应 Yong Quan「我们都在讲老师和家长，忘了学生」这个反思，临时补进讲稿的——这个论证结构（教师协调训练家长 → 家长对孩子放手 → 学生自主性）几乎就是他博士研究两条收敛线索（[[phd-research-direction-clarification]] 记忆里记录的：教师作为家长志工的协调者/训练者、共同目标设定与能力赋权建构信任）的口语化版本，**未来写论文时这段推演过程本身可以回头参考**，讲稿定稿档案（`benchmarking-slot/讲稿要点.md`）建议不要删，当成一份非正式的思路草稿保留

**建议处理方式**：这几份文件目前物理位置分散（deck素材在my-agent底下、研究数据在UTM底下），不用现在急着整并，但下次深入做博士研究工作时，应该主动去读这三份文件，而不是当作跟研究无关的"分享用素材"略过。
